import { View, Text } from 'react-native';

// Placeholder — sera remplacé par l'écran "Choix du pseudo" (input + URL
// preview + suggestions + Popy Intello).
export default function PseudoOnboarding() {
  return (
    <View className="flex-1 items-center justify-center bg-bento-yellow">
      <Text className="font-display text-4xl">Pseudo · TODO</Text>
    </View>
  );
}

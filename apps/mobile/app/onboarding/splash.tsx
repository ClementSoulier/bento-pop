import { View, Text } from 'react-native';

// Placeholder — sera remplacé par l'écran Splash du design (Popy + sticker
// "L'APP COMPANION" + headline "Compose ton bento pop culture").
export default function SplashOnboarding() {
  return (
    <View className="flex-1 items-center justify-center bg-bento-yellow">
      <Text className="font-display text-4xl">Splash · TODO</Text>
    </View>
  );
}

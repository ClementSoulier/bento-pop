import { View, Text } from 'react-native';

// Placeholder — sera remplacé par l'écran "Mécanique" (grille bento vide
// avec annotations FILM PRÉFÉRÉ + UNE CHANSON).
export default function MechanicsOnboarding() {
  return (
    <View className="flex-1 items-center justify-center bg-bento-yellow">
      <Text className="font-display text-4xl">Mécanique · TODO</Text>
    </View>
  );
}

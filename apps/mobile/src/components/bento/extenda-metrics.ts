/**
 * Les largeurs d'Extenda vivent dans le package partagé depuis le chantier 13,
 * pour que l'aperçu de lien de la landing mesure avec la même table que l'app.
 * Ce module garde les imports existants de l'app.
 */
export * from '@bento-pop/supabase-mobile/extenda';

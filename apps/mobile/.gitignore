# Expo
.expo/
dist/
web-build/

# Supabase CLI local state (link, branches, etc.)
supabase/.temp/
supabase/.branches/

# Native
ios/
android/
*.orig.*
*.jks
*.p12
*.key
*.mobileprovision

# Metro / Watchman
.metro-health-check*

# Node
node_modules/

# Caches
.turbo/
.cache/

# OS
.DS_Store
*.log

# Env — Expo lit `.env` directement (contrairement à Next qui ne lit que
# .env*.local). On ignore TOUS les .env locaux. Seul .env.example est commité.
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local
.env.test
.env.test.local

# IDE
.vscode/
.idea/

# Generated (Expo regenerates this on each `expo` command).
expo-env.d.ts
# NB : nativewind-env.d.ts est commité — il déclare le `className` prop
# pour les composants RN. Sans lui, le typecheck CI plante sur les fichiers
# qui utilisent NativeWind.

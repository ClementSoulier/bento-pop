# Configuration Supabase CLI — placeholder.
# Sera initialisée correctement par `supabase init` lors du premier link au
# projet distant. Voir https://supabase.com/docs/guides/cli/config.

project_id = "bento-pop"

[api]
enabled = true
port = 54321
schemas = ["public", "storage"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
